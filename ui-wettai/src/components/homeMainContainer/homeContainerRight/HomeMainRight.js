import React from 'react'
import styled from 'styled-components'

const RightBody = styled.div`
    background: var(--theme-secondary);
    width: var(--width-right-main);
    height: 100%;
`;

const HomeMainRight = () => {
    return(
        <RightBody >
        </RightBody>
    );
}

export default HomeMainRight;